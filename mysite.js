
var age = 26;
var maxAge = 120;
var yearsLeft = maxAge-age;
var amountPerDay = 5;
var daysPerYear = 365;
var amountPerLife = amountPerDay*daysPerYear*yearsLeft;
var answer = "You will need" + amountPerLife + "to last you until your old age of" + maxAge +".";

alert(answer);

if {amountPerLife > 40,000
	alert("Wow! That's a lot!");
};

else if {amountPerLife < 40,000
	alert("You seem pretty reasonable!"")
};


function calculate(){
	
}
